services:
  web:
    build:
      context: .
      target: prod
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DEBUG=False
      - ALLOWED_HOSTS=localhost,127.0.0.1
